---
description: Run PromptLab tests
---

Run the test suite for the PromptLab project.

```bash
cd backend && uv run python -m pytest -v --tb=short
```

Report results and suggest fixes for any failures.
